<h1><%= @tournament.name %></h1>
<br>
<h4>Entry Fee: $<%= @tournament.entry_fee %></h4>

<h4>Players</h4>
<% @players.each do |p| %>
<%= p.name %>
<br>
<% end %>

<h4>Teams</h4>
<h6>Click on Team to edit</h6>
<% @tournament.teams.each do |team|%>
<br>
<%= link_to "Team #{@n+=1}", edit_team_path(team.id) %><br>
-----------------------------------
<br>
<% team.players.each do |player| %>
<%= player.name %>
<br>
<% end %>
<% end %>
<br>
<br>

<%= link_to "Edit Tournament Details", edit_tournament_path(@tournament) %>
<br>
<br>
<%= link_to "Add Golfers to Clubhouse", players_number_path %>
<%= link_to "Add Teams to Tournament", "/teams/number/#{@tournament.id}" %>
<%= link_to "Start Signups", "/tournaments/#{@tournament.id}/start" %>
<%= button_to "Delete Tournament", tournament_path(@tournament), method: :delete %>
